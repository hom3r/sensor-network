'use strict';

const assert = require('assert');

describe('Dummy test suite', () => {
  describe('First test', () => {
    it('should hold that 1 == 1', () => {
      assert.equal(1, 1);
    })
  });
});
